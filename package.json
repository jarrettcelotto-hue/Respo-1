{
  "name": "ai-youtube-autoposter",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "mobile"
  ],
  "scripts": {
    "server:dev": "npm run dev --workspace=server",
    "server:build": "npm run build --workspace=server",
    "mobile:start": "npm run start --workspace=mobile"
  }
}
