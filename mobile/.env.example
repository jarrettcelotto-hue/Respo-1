# Copy to .env. Must point at your running server (see /server/README section).
# Expo inlines any EXPO_PUBLIC_* var into the app bundle at build/start time.
EXPO_PUBLIC_API_URL="http://localhost:4000"
